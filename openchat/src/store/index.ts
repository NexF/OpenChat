import { createStore } from 'vuex';

const state = {
  // 你的 state 属性
};

const mutations = {
  // 你的 mutation 函数
};

const actions = {
  // 你的 action 函数
};

const getters = {
  // 你的 getter 函数
};

const store = createStore({
  state,
  mutations,
  actions,
  getters,
});

export default store;